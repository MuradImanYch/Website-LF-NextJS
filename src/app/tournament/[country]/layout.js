import './style.css';

export const metadata = {
  title: 'sss',
  description: 'w',
  keywords: 'wdw'
};

export default function RootLayout({ children }) {
  return (
    <div>{children}</div>
  );
}
